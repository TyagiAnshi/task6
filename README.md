# task6

We use the Iris dataset to apply KNN for classification. Features are normalized, models are trained for various K values, and performance is evaluated using accuracy and confusion matrices. Finally, decision boundaries are visualized to understand how the model separates classes based on different values of K.
